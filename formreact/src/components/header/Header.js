import React from 'react';
import Logo from '../Logo/Logo';
const Header = () => {
    return (
        <div className='header'>
            <Logo></Logo>
        </div>
    );
};

export default Header;